import { createTheme } from "@rneui/themed";

export const theme = createTheme({
    lightColors: {
        primary: "#007AFF",
        background: "#ffffff",
    },
    darkColors: {
        primary: "#FF9500",
        background: "#000000",
    },
});